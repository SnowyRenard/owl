[package]
name = "owl"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = { version = "*", optional = true, features = [ "derive" ] }

[features]
default = [ "std" ]
std = []
f16 = []
f128 = []
