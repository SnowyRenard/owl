[package]
name = "owl"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = { version = "*", optional = true }

[features]
default = [ "std" ]
std = []
f16 = [ "bytemuck?/nightly_float" ]
f128 = [ "bytemuck?/nightly_float" ]
